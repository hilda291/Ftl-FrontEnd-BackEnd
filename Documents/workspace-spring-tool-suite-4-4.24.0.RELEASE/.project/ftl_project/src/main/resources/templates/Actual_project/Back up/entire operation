Outward Remittance:

Submit Json:
==============
{
    "data": {
        "Remittance": {
            "sendersReference": "TR-001001",
            "valueDateCurrencyInterbankSettledAmount": {
                "valueDate": "2016-02-16",
                "currency": "USD",
                "interbankSettledAmount": 9500.55
            },
            "currencyInstructedAmount": {
                "currency": "USD",
                "amount": 9500.55
            },
            "orderingCustomer": {
                "name": "AMALGA",
                "accountNumber": "",
                "address": ""
            },
            "beneficiaryCustomer": {
                "name": "ABC",
                "accountNumber": "",
                "address": ""
            },
            "remittanceInformation": {
                "information": ""
            },
            "modeOfTransmission": [
                "SW"
            ],
            "detailsOfCharges": [
                "OUR"
            ],
            "additionaldata": {
                "transactionPurpose": ""
            }
        }
    },
    "documents": [
        {
            "metadata": {
                "mimeType": "",
                "extensionType": "",
                "attachment_id": "",
                "description": "",
                "filename": "",
                "type": "",
                "title": "",
                "exported_file_path": "",
                "doc_id": "",
                "dms_id": "",
                "filesize": "",
                "fileUom": "",
                "encrypted": ""
            },
            "docContent": {
                "base64Encoded": ""
            }
        }
    ]
}
=============================
List Json:
==============
{
  "context": "List",
  "customerId": "AMALGA",
  "productCode":"CPCO",
  "filters": [
    {
      "key": "activeCode",
      "operator": "eq",
      "status": "NEW"
    }
  ],
  "pagination": {
    "pageSize": 50,
    "pageIndex": 5
  },
  "sorting": {
    "key": "created_on",
    "value": "asc"
  }
}

=================================
Detail Json:

{
  "action": "findByIdentifier",
  "actionCode": "RetrieveMasterDetail",
  "filterClause": {
    "sendersReference": "ORM-0012"
  },
  "entityCode": "BSIT004"
}

=================================
Ti Url:

http://bsit-srv04:8003/tiplus2-deploy2/hello

DataBase:
======================
Name: TI_ZONE2
IP :bsit-srv04
Port : 1521
SID:xe
UserName:ZONE2
Password:ZONE2


Query for MasterRefID:

SELECT SUBSTR(GWY.ERR_TXT,INSTR(GWY.ERR_TXT,'Master', -1, 1)+7,16) AS MASTER_REF FROM  GWYIN GWY WHERE THEIR_REF ='' And GWY.MESS_TYPE='';

for comma,
<ns2:BeneficiaryAmount>
    <ns3:Amount>${data.Remittance.currencyInstructedAmount.amount?string("0.##")}</ns3:Amount>
    <ns3:Currency>USD</ns3:Currency>
</ns2:BeneficiaryAmount>

======
To achieve the goal of fetching the `MASTER_REF` from the Oracle database and mapping it to the expected JSON format, you can follow these steps:

1. **Create a method to access the database** and execute the query to get the `MASTER_REF`.
2. **Map the fetched `MASTER_REF` to the `channelId` and other JSON fields**.
3. **Return the mapped data in the required JSON format**.

Here’s how you can modify your controller and service classes:

### 1. Modify the Service to Fetch Data from the Database

First, you need to modify the `JsonTOJsonService` to access the database and execute the provided SQL query.

Let’s assume you are using Spring Data JPA or JDBC Template to fetch data from the database. Below is a sample implementation using `JdbcTemplate` to execute the SQL query.

#### Service Class: `JsonTOJsonService.java`

```java
package com.example.ftl_project.Service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Service;

@Service
public class JsonTOJsonService {

    @Autowired
    private JdbcTemplate jdbcTemplate;

    public String getMasterReference(String theirRef, String messType) {
        String sql = "SELECT SUBSTR(GWY.ERR_TXT, INSTR(GWY.ERR_TXT, 'Master', -1, 1) + 7, 16) AS MASTER_REF " +
                     "FROM GWYIN GWY " +
                     "WHERE GWY.THEIR_REF = ? AND GWY.MESS_TYPE = ?";

        // Fetch the MASTER_REF
        return jdbcTemplate.queryForObject(sql, new Object[]{theirRef, messType}, String.class);
    }

    // Add your processTemplate and processMockApi methods here
}
```

### 2. Modify the Controller to Call the Service Method and Map the Values

Next, modify your controller to call the `getMasterReference` method and map the fetched value to the `channelId` and `masterReferenceId` in the expected JSON format.

#### Controller Class: `JsonToJsonController.java`

```java
package com.example.ftl_project.Controller;

import java.nio.file.Files;
import java.nio.file.Paths;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.example.ftl_project.Service.JsonTOJsonService;
import org.json.JSONObject;

@RestController
@RequestMapping("/ftlCheck")
public class JsonToJsonController {

    @Autowired
    private JsonTOJsonService jjService;

    @PostMapping(value = "/JtoX", consumes = "application/json", produces = "application/json")
    public String generateXml(@RequestBody String jsonContent) {
        try {
            // Fetch the `theirRef` and `messType` from the incoming JSON if applicable
            JSONObject json = new JSONObject(jsonContent);
            String theirRef = json.getJSONObject("Remittance").getJSONObject("value").optString("sendersReferenceId", "");
            String messType = json.getJSONObject("Remittance").getJSONObject("value").optString("status", "");

            // Call the service to get the Master Reference
            String masterRef = jjService.getMasterReference(theirRef, messType);

            // Modify the input JSON to include the fetched Master Reference
            JSONObject remittanceJson = json.getJSONObject("Remittance");
            remittanceJson.getJSONObject("value").put("channelId", masterRef);
            remittanceJson.getJSONObject("value").put("masterReferenceId", masterRef);

            // Process the template with the updated JSON content
            String ftlContent = new String(Files.readAllBytes(Paths.get("D:\\Bluescope\\.project\\ftl_project\\src\\main\\resources\\templates\\Actual_project\\toXML_create.ftl")));
            String xmlOutput = jjService.processTemplate(jsonContent, ftlContent);
            System.out.println("Generated XML: " + xmlOutput);

            String ftlFilePath = new String(Files.readAllBytes(Paths.get("D:\\Bluescope\\.project\\ftl_project\\src\\main\\resources\\templates\\Actual_project\\toJson_create.ftl")));
            String result = jjService.processMockApi("http://bsit-srv04:8003/tiplus2-deploy2/hello", xmlOutput, ftlFilePath);

            // Return the final result (could be JSON or XML, depending on the API)
            return result;

        } catch (Exception e) {
            e.printStackTrace();
            return "Error processing template: " + e.getMessage();
        }
    }
}
```

### 3. Database Configuration for JdbcTemplate

Make sure that you have the necessary database configuration set up in your `application.properties` or `application.yml`.

#### `application.properties`:

```properties
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:ORCL
spring.datasource.username=your_username
spring.datasource.password=your_password
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.platform=oracle
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# JdbcTemplate Configuration
spring.datasource.initialization-mode=always
```

Make sure your database credentials and Oracle driver are correctly configured.

### 4. Final JSON Format

After calling the method to fetch `MASTER_REF` from the database, your response will look something like this:

```json
{
  "Remittance": {
    "value": {
      "sendersReferenceId": " ",
      "channelId": "MASTER_REF_FROM_DB",
      "masterReferenceId": "MASTER_REF_FROM_DB",
      "status": " ",
      "message": " "
    }
  }
}
```

Where `MASTER_REF_FROM_DB` is the value fetched from the Oracle database by the query:

```sql
SELECT SUBSTR(GWY.ERR_TXT, INSTR(GWY.ERR_TXT, 'Master', -1, 1) + 7, 16) AS MASTER_REF 
FROM GWYIN GWY 
WHERE GWY.THEIR_REF = '' AND GWY.MESS_TYPE = '';
```

### Summary of Changes:
1. **Service Layer**: Created a method `getMasterReference()` using `JdbcTemplate` to run the SQL query and retrieve the `MASTER_REF`.
2. **Controller**: Modified to call `getMasterReference()` and update the `channelId` and `masterReferenceId` fields in the JSON request before processing it further.
3. **Database Configuration**: Ensure `JdbcTemplate` is properly configured to connect to your Oracle database.

Now, when the API is called, it will:
1. Retrieve the `MASTER_REF` from the database.
2. Insert this value into the `channelId` and `masterReferenceId` fields of the JSON.
3. Process the updated JSON and generate the required XML output.

=======
for correlation isd:
Ah, I see! You want to assign a **correlation ID** to the `channelId` in your code. This can be done in several ways, but since you want to specify it in the code directly and then assign that value to the `channelId`, we can modify the logic to achieve this.

### Plan
- **Correlation ID**: We will generate or specify a fixed value for the `channelId` (correlation ID) directly in the code.
- **Update JSON**: We'll insert the generated `channelId` into the `channelId` field in the JSON.

### 1. Modify the Controller to Set the `channelId` (correlation ID)

We'll modify the controller to set the `channelId` manually, either as a hardcoded value or generate it dynamically (for example, using a UUID).

Here’s how you can modify the controller class to set the `channelId` (correlation ID):

#### Modified Controller Class: `JsonToJsonController.java`

```java
package com.example.ftl_project.Controller;

import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.UUID;  // For generating a unique correlation ID

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.example.ftl_project.Service.JsonTOJsonService;
import org.json.JSONObject;

@RestController
@RequestMapping("/ftlCheck")
public class JsonToJsonController {

    @Autowired
    private JsonTOJsonService jjService;

    @PostMapping(value = "/JtoX", consumes = "application/json", produces = "application/json")
    public String generateXml(@RequestBody String jsonContent) {
        try {
            // Fetch the `theirRef` and `messType` from the incoming JSON (if applicable)
            JSONObject json = new JSONObject(jsonContent);
            String theirRef = json.getJSONObject("Remittance").getJSONObject("value").optString("sendersReferenceId", "");
            String messType = json.getJSONObject("Remittance").getJSONObject("value").optString("status", "");

            // Generate or specify the Correlation ID (channelId)
            // You can hardcode a value or generate it dynamically, for example, using UUID
            String correlationId = generateCorrelationId();  // This method generates the correlation ID

            // Assign the generated correlation ID to the channelId field
            JSONObject remittanceJson = json.getJSONObject("Remittance");
            remittanceJson.getJSONObject("value").put("channelId", correlationId);  // Set the channelId (correlationId)
            remittanceJson.getJSONObject("value").put("masterReferenceId", correlationId); // Optionally set this as well

            // Optionally, if you need to get the Master Reference from the database, uncomment and use the following:
            // String masterRef = jjService.getMasterReference(theirRef, messType);
            // remittanceJson.getJSONObject("value").put("masterReferenceId", masterRef);

            // Process the template with the updated JSON content
            String ftlContent = new String(Files.readAllBytes(Paths.get("D:\\Bluescope\\.project\\ftl_project\\src\\main\\resources\\templates\\Actual_project\\toXML_create.ftl")));
            String xmlOutput = jjService.processTemplate(jsonContent, ftlContent);
            System.out.println("Generated XML: " + xmlOutput);

            // Specify the path for the FTL file (for the mock API call)
            String ftlFilePath = new String(Files.readAllBytes(Paths.get("D:\\Bluescope\\.project\\ftl_project\\src\\main\\resources\\templates\\Actual_project\\toJson_create.ftl")));
            
            // Call the mock API with the generated XML output and FTL path
            String result = jjService.processMockApi("http://bsit-srv04:8003/tiplus2-deploy2/hello", xmlOutput, ftlFilePath);

            // Return the final result (could be JSON or XML, depending on the API)
            return result;

        } catch (Exception e) {
            e.printStackTrace();
            return "Error processing template: " + e.getMessage();
        }
    }

    // Method to generate a unique correlation ID (channelId)
    private String generateCorrelationId() {
        // Here, we're using UUID to generate a random unique ID
        return UUID.randomUUID().toString();  // This generates a new unique ID each time
    }
}
```

### Key Changes:
1. **Correlation ID Generation**: 
   - The `generateCorrelationId()` method generates a unique correlation ID using `UUID.randomUUID().toString()`. 
   - You can replace this logic if you want a different approach for generating or specifying the correlation ID.

2. **Setting `channelId`**: 
   - The `channelId` field in the JSON is set to the generated `correlationId`.
   - You also have the option to set `masterReferenceId` to the `correlationId` if that’s your requirement.

### 2. Updated JSON Output

After calling the `generateXml()` endpoint, the returned JSON will have the `channelId` (correlation ID) set as per the generated value.

For example, if the `UUID.randomUUID()` generates `"d9f0a9c9-347f-4d4f-b9f1-2fcd5298d865"`, the output JSON will look like this:

```json
{
  "Remittance": {
    "value": {
      "sendersReferenceId": " ",
      "channelId": "d9f0a9c9-347f-4d4f-b9f1-2fcd5298d865",  // This is the generated correlation ID
      "masterReferenceId": "d9f0a9c9-347f-4d4f-b9f1-2fcd5298d865", // This can also be set as the correlation ID
      "status": " ",
      "message": " "
    }
  }
}
```

### 3. Optional: Hardcoding the `channelId`

If you prefer to hardcode a specific `channelId` (correlation ID) instead of generating it dynamically, you can replace the `generateCorrelationId()` method with a fixed value like this:

```java
private String generateCorrelationId() {
    // Hardcoding a specific correlation ID
    return "CORRELATION_ID_123456789";
}
```

### Summary:
- We have updated the controller to generate a unique **correlation ID** (via `UUID.randomUUID()`) and assign it to the `channelId` field in the JSON.
- The correlation ID is then used in the **`Remittance.value.channelId`** field, and optionally can be used in **`masterReferenceId`**.
- You can replace the dynamic generation of the correlation ID with a hardcoded value if needed.

Let me know if this solution meets your requirements or if you need any further adjustments!
